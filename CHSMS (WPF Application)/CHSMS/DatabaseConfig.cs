using System;

namespace CHSMS
{
    public static class DatabaseConfig
    {
        // Paste your exact copied string inside the quotes below
        public static string ConnectionString = @"Server=(localdb)\MSSQLLocalDB;Database=CHSMS;Trusted_Connection=True;";
    }
}